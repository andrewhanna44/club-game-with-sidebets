# Whitelist: only the app itself goes to this PUBLIC repo.
# Source spreadsheet, the club's email, the decision log, and local
# tooling stay on this machine only.
*
!.gitignore
!index.html
!database.rules.json
